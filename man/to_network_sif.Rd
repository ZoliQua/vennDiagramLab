% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network-export.R
\name{to_network_sif}
\alias{to_network_sif}
\alias{to_network_sif,RegionResult-method}
\title{Write the Cytoscape SIF network export}
\usage{
to_network_sif(result, path)

\S4method{to_network_sif}{RegionResult}(result, path)
}
\arguments{
\item{result}{A [`RegionResult-class`].}

\item{path}{Destination file path.}
}
\value{
Invisibly returns `path`.
}
\description{
Mirrors the React webapp's "SIF" Cytoscape export button + Python's
`to_network_sif()` byte-for-byte. One line per edge (in edge order),
tab-separated: source letter, the literal interaction type `overlap`, then
target letter. Isolated nodes (degree 0) are emitted as lone single-token
lines after all edges, in node order.
}
\examples{
ds <- methods::new("VennDataset",
    set_names = c("A", "B"),
    items = list(A = c("x", "y"), B = c("y", "z")),
    item_order = c("x", "y", "z"),
    universe_size = 10L, source_path = NULL, format = "csv")
result <- analyze(ds)
to_network_sif(result, tempfile(fileext = ".sif"))
\donttest{
result <- analyze(load_sample("dataset_real_cancer_drivers_4"))
to_network_sif(result, tempfile(fileext = ".sif"))
}
}
